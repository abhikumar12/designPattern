// 🎯 EXERCISE — two more classic binary-search problems.
// Copy pattern.js above this comment (or edit it directly), then:
//
// TODO 1: `searchRange(nums, target)` — given a sorted array WITH DUPLICATES, find the first
//         AND last index of `target` (return [-1, -1] if absent). Run two separate binary
//         searches: one biased to keep moving LEFT even after finding a match (to find the
//         first occurrence), one biased to keep moving RIGHT (to find the last).
//
// TODO 2: `mySqrt(x)` — implement integer square root WITHOUT using Math.sqrt. This is
//         "binary search on the answer" again: search over candidate answers from 0 to x,
//         checking `candidate * candidate <= x` as the condition, same shape as
//         minEatingSpeed()'s `hoursNeeded(mid) <= h` check.
//
// Neeche apna solution likho, phir `node exercise.js` chala kar verify karo.
