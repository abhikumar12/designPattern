// 🎯 EXERCISE — two harder two-pointer problems.
// Copy pattern.js above this comment (or edit it directly), then:
//
// TODO 1: `threeSum(nums)` — find all UNIQUE triplets in `nums` that sum to 0. Sort the
//         array first. Fix one number, then use two-pointers on the rest (exactly like
//         twoSumSorted, just targeting `-nums[i]` instead of a fixed target). Skip duplicate
//         values for `i`, `left`, and `right` to avoid duplicate triplets in the output.
//
// TODO 2: `maxArea(heights)` — "container with most water": given heights of vertical
//         lines, find two lines that, together with the x-axis, form the container holding
//         the most water. Start with pointers at both ends (widest possible container), and
//         at each step move whichever pointer points to the SHORTER line inward (moving the
//         taller one can only shrink the width without ever increasing the height, so it
//         can never improve the answer — moving the shorter one is the only move that
//         could).
//
// Neeche apna solution likho, phir `node exercise.js` chala kar verify karo.
